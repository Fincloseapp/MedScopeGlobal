import type { Article, MedicalEvent } from "./types";
import { siteConfig } from "./site";
export function organizationJsonLd() { return { "@context": "https://schema.org", "@type": "Organization", name: siteConfig.name, url: siteConfig.url, description: siteConfig.description, email: siteConfig.contactEmail, contactPoint: [{ "@type": "ContactPoint", email: siteConfig.contactEmail, contactType: "customer support" }, { "@type": "ContactPoint", email: siteConfig.adsEmail, contactType: "sales" }] }; }
export function articleJsonLd(article: Article) { return { "@context": "https://schema.org", "@type": "Article", headline: article.title, description: article.summary, author: { "@type": "Person", name: article.author }, publisher: { "@type": "Organization", name: siteConfig.name, email: siteConfig.contactEmail }, datePublished: article.date, sourceOrganization: article.source, isBasedOn: article.sourceUrl, audience: article.audience, mainEntityOfPage: `${siteConfig.url}/articles/${article.slug}` }; }
export function eventJsonLd(event: MedicalEvent) { return { "@context": "https://schema.org", "@type": "Event", name: event.title, description: event.description, startDate: event.startsAt, endDate: event.endsAt, eventAttendanceMode: event.format === "online" ? "https://schema.org/OnlineEventAttendanceMode" : "https://schema.org/MixedEventAttendanceMode", eventStatus: "https://schema.org/EventScheduled", location: { "@type": event.format === "online" ? "VirtualLocation" : "Place", name: event.venue || event.format, url: event.registrationUrl || `${siteConfig.url}/events/${event.slug}` }, organizer: { "@type": "Organization", name: event.organizer, email: siteConfig.contactEmail }, url: `${siteConfig.url}/events/${event.slug}` }; }
