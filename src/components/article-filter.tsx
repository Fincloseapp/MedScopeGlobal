"use client";
import { FormEvent } from "react";
import { useRouter, useSearchParams } from "next/navigation";
import { articleAudiences, regions, specializations } from "@/lib/data";
export function ArticleFilter() { const router = useRouter(); const params = useSearchParams(); function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); const next = new URLSearchParams(); for (const [key, value] of data.entries()) if (String(value)) next.set(key, String(value)); router.push(`/articles${next.size ? `?${next}` : ""}`); } return <form className="filter-bar article-filter" onSubmit={submit}><input name="q" placeholder="Hledat článek, autora nebo zdroj" defaultValue={params.get("q") ?? ""} /><select name="specialization" defaultValue={params.get("specialization") ?? ""}><option value="">Všechny specializace</option>{specializations.map((item) => <option key={item} value={item}>{item}</option>)}</select><select name="region" defaultValue={params.get("region") ?? ""}><option value="">Všechny regiony</option>{regions.map((item) => <option key={item} value={item}>{item}</option>)}</select><select name="audience" defaultValue={params.get("audience") ?? ""}><option value="">Všechny úrovně</option>{articleAudiences.map((item) => <option key={item.value} value={item.value}>{item.label}</option>)}</select><button className="button" type="submit">Filtrovat</button></form>; }
