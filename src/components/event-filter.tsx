"use client";
import { FormEvent } from "react";
import { useRouter, useSearchParams } from "next/navigation";
import { regions, specializations } from "@/lib/data";
export function EventFilter() { const router = useRouter(); const params = useSearchParams(); function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); const next = new URLSearchParams(); for (const [key, value] of data.entries()) if (String(value)) next.set(key, String(value)); router.push(`/events${next.size ? `?${next}` : ""}`); } return <form className="filter-bar" onSubmit={submit}><input name="q" placeholder="Hledat událost nebo organizátora" defaultValue={params.get("q") ?? ""} /><select name="region" defaultValue={params.get("region") ?? ""}><option value="">Region</option>{regions.map((item) => <option key={item} value={item}>{item}</option>)}</select><select name="format" defaultValue={params.get("format") ?? ""}><option value="">Formát</option><option value="online">Online</option><option value="hybrid">Hybridní</option><option value="in-person">Prezenčně</option></select><select name="specialization" defaultValue={params.get("specialization") ?? ""}><option value="">Specializace</option>{specializations.map((item) => <option key={item} value={item}>{item}</option>)}</select><button className="button" type="submit">Filtrovat</button></form>; }
