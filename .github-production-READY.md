# Produkční patch — připraven k nasazení

Změny v `github-production/` (12 souborů) odpovídají repozitáři **Fincloseapp/MedScopeGlobal** (`src/`).

## Co se na webu změní

- Homepage: **Laik a student · Lékař · Vědec** (místo Klinici/Instituce/B2B)
- Navigace v češtině: Články, Pro koho, Medicína, Registrace
- Routy: `/pro-koho/*`, `/medicina`, `/medicina/priprava`, `/medicina/studium`
- Oprava `/sitemap.xml` (statické URL)

## Nasazení (1 příkaz)

```powershell
powershell -ExecutionPolicy Bypass -File C:\Users\zegzulka\MedScopeGlobal\NASADIT.ps1
```

Vyžaduje v `.env.local` proměnnou `GITHUB_TOKEN` (viz `.env.local.example`).

Lokální commit je připraven v `.tools\MedScopeGlobal` — skript pushne na `main`, Vercel nasadí automaticky.
