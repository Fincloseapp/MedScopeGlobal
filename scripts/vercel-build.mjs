#!/usr/bin/env node
/**
 * Retired. Production build is OpenNext → Cloudflare Workers (`npm run build` / `cf:build`).
 */
console.error("Vercel build wrapper is retired. Use npm run build (OpenNext / Cloudflare).");
process.exit(1);
