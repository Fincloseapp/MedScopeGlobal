#!/usr/bin/env node
/**
 * Retired. Production is Cloudflare Workers — set GROQ keys via
 * `npm run cf:env:sync` / Worker secrets, not Vercel.
 */
console.error("Vercel env sync is retired. Production is Cloudflare Workers.");
process.exit(1);
