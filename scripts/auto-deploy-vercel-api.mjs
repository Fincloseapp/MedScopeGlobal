#!/usr/bin/env node
/**
 * Retired. Production is Cloudflare Workers (OpenNext), Worker medscopeglobal.
 */
console.error("Vercel API deploy is retired. Production is Cloudflare Workers (npm run deploy).");
process.exit(1);
