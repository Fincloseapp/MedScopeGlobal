#!/usr/bin/env node
/**
 * Retired. V17 Vercel auto-deploy is gone; production is Cloudflare Workers.
 */
console.error("V17 Vercel pipeline is retired. Production is Cloudflare Workers (npm run deploy).");
process.exit(1);
