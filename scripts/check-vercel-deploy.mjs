#!/usr/bin/env node
/**
 * Retired. Production is Cloudflare Workers — use `npm run cf:smoke`.
 */
console.error("Vercel deploy check is retired. Production is Cloudflare Workers.");
process.exit(1);
