#!/usr/bin/env node
/**
 * Retired. Production is Cloudflare Workers — use Workers Builds logs.
 */
console.error("Vercel build-log fetch is retired. Production is Cloudflare Workers.");
process.exit(1);
