#!/usr/bin/env node
/**
 * Retired. Production is Cloudflare Workers (cloudflare-deploy.yml / Workers Builds).
 */
console.error("Vercel Git status check is retired. Production is Cloudflare Workers.");
process.exit(1);
